# Frontend

This directory will host the React + Vite TypeScript application that consumes the backend API.

## Planned Work

- Scaffold a Vite project configured for React and TypeScript.
- Implement shared UI components and state management solutions as needed.
- Integrate API clients and shared type definitions from the backend.
