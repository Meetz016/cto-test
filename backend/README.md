# Backend

This directory will host the Express + TypeScript API, along with Prisma for database access.

## Planned Work

- Initialize a TypeScript configuration for the service.
- Configure Prisma schema and migrations targeting SQLite.
- Expose REST endpoints to be consumed by the frontend client.
